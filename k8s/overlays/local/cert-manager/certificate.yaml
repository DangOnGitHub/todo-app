apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: todo-app-tls
  namespace: todo-app
spec:
  secretName: todo-app-tls
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
  dnsNames:
    - localhost
